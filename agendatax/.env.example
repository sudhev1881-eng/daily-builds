# Public site URL used for canonical links and Open Graph metadata
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Set to 1 for GitHub Pages static export builds
NEXT_PUBLIC_STATIC_EXPORT=

# Base path when hosted under a subpath (e.g. /daily-builds)
NEXT_PUBLIC_BASE_PATH=
